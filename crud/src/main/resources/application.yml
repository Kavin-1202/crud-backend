server:
  port: 9090
spring:
  application:
    name: crud
  datasource:
    url: jdbc:mysql://localhost:3306/crud
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true